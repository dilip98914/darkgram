<%- include("./partials/header") %>
<div class="w-full min-h-screen bg-zinc-900 px-4 py-5">
    <div class="border-2 border-zinc-800 flex items-center justify-between px-2 
    py-1 rounded-md">
        <i class="input-white ri-search-line"></i>
        <input class="input ml-1 w-full bg-zinc-900 outline-none text-zinc-400" type="text"
        placeholder="search username"
        >
    </div>
    <div class="users">
    </div>

</div>
<script>
    const inp=document.querySelector('.input');
    inp.addEventListener('input',(e)=>{
        fetch(`/search/${inp.value}`)
            .then(raw=>raw.json())
            .then(result=>{
                let clutter="";
                console.log('aaaaaaaa',result)
            })
    })

</script>


<%- include("./partials/footer") %>